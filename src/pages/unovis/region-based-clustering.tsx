import React from 'react';
import GraphComponent from '@app/components/unovis/region-based-clustering';

const RegionBasedClustering: React.FC = () => {
  return <GraphComponent />;
};

export default RegionBasedClustering;
